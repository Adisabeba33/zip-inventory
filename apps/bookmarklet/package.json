{
  "name": "@inventory-index/bookmarklet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "echo 'plain JS, nothing to typecheck'",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@inventory-index/core": "*"
  }
}
